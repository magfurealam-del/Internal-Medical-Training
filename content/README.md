# Course content

Store learner-facing Markdown lesson files here. The `content_path` on each row in `training.lessons` should point to a file inside this directory, for example:

```text
courses/clinical-team-essentials/module-01/lesson-01.md
```

Content changes should go through GitHub pull requests and Vercel preview deployments.
