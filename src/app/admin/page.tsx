import Link from "next/link";
import { listAllCourses } from "@/lib/training/courses";
import CourseCreateForm from "./CourseCreateForm";

export default async function AdminPage() {
  const courses = await listAllCourses();
  return <main className="mx-auto max-w-7xl px-6 py-16 lg:px-10"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-[#007c8b]">Administration</p><h1 className="mt-3 text-5xl font-semibold tracking-tight text-[#002f65]">Platform overview</h1><p className="mt-4 max-w-xl text-[#526b78]">Manage the training catalogue and prepare courses for publication.</p><div className="mt-10 grid gap-5 lg:grid-cols-[1fr_0.75fr]"><section className="rounded-2xl bg-white p-6 ring-1 ring-[#d5e9ed]"><div className="flex items-center justify-between"><h2 className="text-2xl font-semibold text-[#002f65]">Courses</h2><span className="text-sm text-[#526b78]">{courses.length} total</span></div>{courses.length === 0 ? <p className="mt-8 rounded-xl bg-[#f6feff] p-6 text-sm text-[#526b78]">No courses created yet.</p> : <div className="mt-6 space-y-3">{courses.map((course) => <Link key={course.id} href={`/admin/courses/${course.id}`} className="flex items-center justify-between rounded-xl border border-[#d5e9ed] p-4 transition hover:border-[#007c8b]"><div><h3 className="font-semibold text-[#002f65]">{course.title}</h3><p className="mt-1 text-xs text-[#526b78]">{course.slug}</p></div><span className="rounded-full bg-[#d9f2f4] px-3 py-1 text-xs font-medium text-[#007c8b]">{course.status}</span></Link>)}</div>}</section><section className="rounded-2xl bg-[#002f65] p-6 text-white"><h2 className="text-2xl font-semibold">Create a course</h2><p className="mt-2 text-sm leading-6 text-white/65">Start with metadata here; curriculum will remain managed in GitHub/MDX.</p><CourseCreateForm /></section></div></main>;
}
